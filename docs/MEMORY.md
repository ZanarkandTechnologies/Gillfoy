2026-03-18 20:26 +0000 | decision | MEM-0001 | skills,debugging | Reproducible runtime issues should trigger the dedicated `runtime-debugging` skill, which starts with explicit hypotheses and runtime evidence collection before fixes. UI-first visual issues still route to `visual-qa`.
2026-03-18 22:39 +0000 | decision | MEM-0003 | skills,debugging | `runtime-debugging` should stay minimal at the top level and route specific debugging entry modes to focused reference playbooks instead of embedding all strategies in `SKILL.md`.
2026-03-18 20:38 +0000 | decision | MEM-0002 | skills,planning,qa | Superseded by MEM-0006. UI-bearing tickets need explicit agent-facing QA and delegation context; the current contract now packages that context into one compact `Agent Contract` block.
2026-03-18 22:39 +0000 | decision | MEM-0004 | skills,design,planning | Shared visual doctrine belongs in project-level `docs/TASTE.md`; tickets should reference taste briefly and only add local overrides instead of restating style heuristics inline.
2026-03-18 22:39 +0000 | decision | MEM-0005 | skills,qa,design | `visual-qa` must derive expected UI from ticket-declared screens/states plus `docs/TASTE.md`; if those expectations are missing, QA should fail as underspecified rather than reward-hack route completion.
2026-03-18 22:39 +0000 | decision | MEM-0006 | skills,planning,qa | UI-bearing tickets should use one compact `Agent Contract` block instead of separate QA/delegation/testability sections, as long as the block still captures open path, stabilization, inspection, taste refs, proof, and delegation artifact context.
2026-03-18 22:52 +0000 | decision | MEM-0007 | skills,planning | `tech-impl-plan` should optimize for approval speed with a compact top section: `Pitch`, `B -> A`, `Delta`, `Core Flow`, `Proof`, `Ask`; diagrams and deep rationale are optional appendix content.
2026-03-18 22:52 +0000 | decision | MEM-0008 | repo,planning | Root `AGENTS.md` should stay terse and repo-only; detailed planning mechanics belong in skills, not the root contract.
2026-03-18 22:56 +0000 | decision | MEM-0009 | skills,git,repo | Commit subjects should default to compact Conventional-style form: `type(scope): lower-case imperative summary`; the output should lead with one best subject and only show alternates when the diff is genuinely ambiguous.
2026-03-18 23:05 +0000 | decision | MEM-0010 | skills,qa,planning | UI verification should follow a ticket evidence loop: tickets declare a short `Evidence Checklist`, `qa-tester` reconciles ACs and screens against artifacts, and `visual-qa` should judge captured evidence rather than ad-hoc screenshots.
2026-03-18 23:15 +0000 | decision | MEM-0011 | tickets,planning,qa | The canonical execution board should live in the filesystem under `tickets/`: raw work starts in `todo`, active planning happens in `review`, approved work runs from `building`, and only user-checked merged work moves to `done`.
2026-03-18 23:18 +0000 | decision | MEM-0012 | tickets,autonomy,planning | Autonomous work should follow a ticket state machine: agents update ticket control fields, move tickets across `todo -> review -> building -> done`, record blockers in the ticket, and spawn linked follow-up tickets instead of hiding overflow scope in prose.
2026-03-18 23:24 +0000 | decision | MEM-0013 | tickets,qa,handoff | Every autonomous ticket loop should end with a user-facing evidence packet in the ticket itself: hero screenshot, supporting evidence, QA report link, and a short verdict. Execution blockers stay in `building`; planning/scope blockers move back to `review`.
2026-03-18 23:40 +0000 | decision | MEM-0014 | prompts,contracts,qa | Hot-path agent prompts should stay minimal: `AGENTS.md` carries the embedded system map plus guardrails, build prompts carry only execution steps, `qa-tester` owns evidence capture and ticket write-back, and `visual-qa` owns visual judgment only.
2026-03-18 23:49 +0000 | decision | MEM-0015 | prompts,contracts,qa | Human overview docs should point to canonical templates rather than restating mutable field lists, and top-level QA skills should keep runbooks in references so responsibility boundaries stay clear after prompt compression.
2026-03-18 23:36 +0000 | decision | MEM-0014 | repo,skills,feedback | Repeated misses and user corrections should be captured first in `docs/TROUBLES.md` as raw operator feedback; only stable lessons should then be promoted into `docs/MEMORY.md`, `AGENTS.md`, or the relevant skill.
2026-03-19 00:03 +0000 | decision | MEM-0016 | planning,quality,skills | Every `tech-impl-plan` output should run a built-in quality review before handoff and report the result explicitly, so approval-ready plans do not skip reference coverage, hide multi-commit scope, or rely on vague proof/rollback notes.
2026-03-19 00:16 +0000 | decision | MEM-0017 | skills,review,quality | Each planning/review-phase skill should keep its own short `references/review.md` and explicitly require consulting it before handoff, so phase-specific quality questions improve outputs without bloating top-level `SKILL.md` files.
2026-03-19 00:34 +0000 | decision | MEM-0018 | planning,qa,testability | Agent-facing testability should be explicit in the ticket before build starts: every non-trivial ticket declares a `Test hook` (CLI command, seed/reset path, debug route, sanity script, fixture loader, or `none needed`), and QA should fail early when that surface is missing but obviously required.
2026-03-19 00:41 +0000 | decision | MEM-0019 | delegation,tickets,planning | Delegation should be ticket-file-first across the system: create or select the ticket first, pass the ticket path/reference to the delegated agent, keep the prompt secondary, and require write-back into that same ticket as part of completion.
